server:
  port: 8888

spring:
  application:
    name: config-server-service
  cloud:
    config:
      server:
        git:
          uri: ${user.home}/Documents/dev/workshop
          skipSslValidation: true
          search-paths: /source/java/workshop/workshop-services-configurations

eureka:
  client:
    enabled: false
    serviceUrl:
      defaultZone: http://localhost:8082/eureka/

#management:
#  endpoints:
#    web:
#      exposure:
#        include: "*"



